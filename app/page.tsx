import Link from "next/link";import ContactForm from "@/components/ContactForm";import {clsx} from "clsx";import {allPosts, allProjects} from "contentlayer/generated";import PostDetail from "@/components/PostDetail";import {compareDesc} from "date-fns";import ProjectDetail from "@/components/ProjectDetail";export default async function Home() {    const projects = allProjects.sort((a, b) => compareDesc(new Date(a.date), new Date(b.date)))    return (        <div>                <h1 className="pb-8 pt-4 text-2xl font-bold leading-[1] tracking-tighter hover:animate-pulse sm:text-4xl md:text-5xl">                    Hello, friend.                </h1>                <p className="justify-evenly leading-relaxed text-slate-700 pb-6">                    I&apos;m{" "}                    <span className="rounded-full border-slate-700 bg-slate-900 p-1 font-bold text-gray-50">                        Jason Vlajankov                    </span>                    , a                    {/* <span className="rounded-full border-slate-700 bg-slate-900 p-1 font-bold text-gray-50"> */}                    {/*   full stack  */}                    {/* </span> { // uncomment when become full stack} */} software                    developer based in <i>Melbourne, Australia</i>. At the moment, I&apos;m                    focused on learning more about artificial intelligence and full stack development.                </p>                <p className="border-t border-slate-400 py-6">                    <span className="rounded-lg border-slate-300 p-1 font-mono">                        jvlaj.com{""}                    </span>{" "}                    is my personal website where I share my thoughts and projects.{" "}                </p>        </div>    );}